# HPC 智能体 Demo：设计师与 Agent 协作指南

## 1. 项目目标

本项目分为两个阶段：

1. 建立一套稳定、可复用的 HPC 智能体 Demo 基础框架。
2. 基于该框架，持续探索和验证不同的智能体体验概念。

协作的核心不是让 Agent 独立完成设计，而是建立清晰分工：

| 设计师负责 | Agent 负责 |
|---|---|
| 问题定义与用户价值 | 阅读材料、整理信息和实现代码 |
| HPC 领域知识与业务判断 | 将领域知识转换为数据模型和界面 |
| 智能体权限及信任边界 | 快速生成可运行原型 |
| 视觉、交互和信息架构判断 | 方案推演、细节补齐和批量修改 |
| 优先级、范围及最终取舍 | 定位技术问题并执行验证 |
| 验收标准 | 按验收条件迭代实现 |

> 设计师掌握“为什么、为谁做、什么才算正确”；Agent 负责把这些判断快速转换成可运行、可比较、可评审的产物。

---

## 2. 第一阶段：搭建基础框架

第一阶段不应急于探索大量创新点。应先建立一个能承载后续概念的“HPC 智能体工作台”。

### 2.1 建议包含的基础区域

- 全局导航：集群、作业、资源、告警、知识等。
- 对象上下文：当前集群、作业、节点、分区、队列和用户。
- Agent 任务输入区。
- 执行计划、步骤或检查点。
- 工具调用和数据查询过程。
- 当前状态、等待原因、耗时和进度。
- 证据、数据来源和更新时间。
- 权限确认及影响范围预览。
- 分析结论、建议和可执行操作。
- 任务历史、中断恢复和结果追溯。
- 页面、图表、对象和 Agent 对话之间的联动。

### 2.2 不要只设计“右侧聊天栏”

HPC 智能体的核心通常是一条任务闭环：

```text
用户提出目标
→ Agent 识别当前对象和环境
→ 制订计划
→ 查询调度器、监控、日志或知识库
→ 形成判断并展示证据
→ 请求执行授权
→ 执行动作
→ 验证执行结果
→ 留下可追溯记录
```

基础框架需要能够表现完整闭环，而不是只展示用户与 Agent 的文字对话。

### 2.3 第一阶段完成标准

- 基础外壳能稳定承载不同 HPC 场景。
- 页面能明确表达 Agent 当前在做什么、为什么等待、用了什么数据。
- 事实、推断、建议和已执行动作可以清楚区分。
- 至少覆盖成功、失败、数据不足、权限不足、中断和恢复状态。
- 后续新增概念不需要重新制作导航、工作区和 Agent 通用组件。
- 设计 Token、公共组件和示例数据已经形成统一来源。

---

## 3. 第二阶段：概念方案探索

基础框架稳定后，再按照“一项概念、一个独立 Demo”的方式探索。

```text
concepts/
├── 01-job-failure-diagnosis/
├── 02-resource-request-tuning/
├── 03-queue-selection/
├── 04-cluster-health-inspection/
├── 05-capacity-forecast/
├── 06-interactive-troubleshooting/
├── 07-agent-team/
└── 08-execution-approval/
```

每个概念应该只回答一个明确问题，例如：

- Agent 如何解释作业为什么排队？
- 用户如何比较不同资源申请方案？
- 诊断证据如何与日志、指标和拓扑联动？
- 长时间任务如何让用户感知过程及等待原因？
- 哪些操作可以自动执行，哪些必须由用户确认？
- 多 Agent 协作对用户是否产生实际价值？
- Agent 中断、失败或结论不足时如何恢复？
- 如何区分分析结论、操作建议和实际动作？

建议每个概念具备：

- 独立 URL；
- 独立缩略图；
- 概念说明及要验证的假设；
- 主要操作路径；
- 状态覆盖说明；
- 已验证、推断和待确认内容；
- 与其他概念一致的共享外壳和设计系统。

---

## 4. 需要长期传递给 Agent 的项目上下文

以下内容应该进入仓库，成为 Agent 每次工作时都可以读取的长期上下文，而不是只存在于聊天记录中。

### 4.1 产品与用户语境

```markdown
产品是什么：
目标用户：
典型使用环境：
用户最重要的任务：
用户当前如何完成：
主要困难：
业务目标：
Demo 用于验证什么：
```

不要将用户笼统定义为“HPC 用户”或“运维人员”。建议至少区分：

- 普通科研用户；
- AI/模型训练用户；
- 应用工程师；
- 集群管理员；
- 调度管理员；
- 平台运维人员；
- 技术支持人员；
- 管理决策者。

不同角色拥有不同知识水平、操作权限、证据需求和自动化接受程度。

### 4.2 HPC 概念词典

需要向 Agent 明确定义：

- Cluster、Partition、Queue 的关系；
- Job、Step、Task、Process 的关系；
- Node、CPU、GPU、Memory 等资源单位；
- Pending、Running、Failed 等状态的业务含义；
- 调度优先级及其影响因素；
- 独占、共享和超售等资源规则；
- 诊断、建议、模拟和执行的区别；
- 产品内部术语与行业通用术语的差异。

领域词典用于避免 Agent 生成“看起来像 HPC、实际语义不正确”的界面。

### 4.3 对象与数据模型

建议提供明确的数据关系和脱敏样例：

```text
Cluster
├── Partitions
├── Nodes
├── Jobs
├── Alerts
└── Metrics

Job
├── identity
├── owner
├── queue / partition
├── requested resources
├── allocated resources
├── status and reason
├── timeline
├── logs
├── metrics
└── related jobs
```

只提供几个字段名称，Agent 通常只能生成通用 Dashboard。具有真实结构、数量级、单位、异常和边界情况的脱敏数据，才足以支持专业工具设计。

### 4.4 Agent 能力、工具及风险边界

建议建立工具清单：

| 工具 | 读/写 | 风险 | 是否需要确认 |
|---|---|---:|---|
| 查询作业 | 读 | 低 | 否 |
| 查询节点指标 | 读 | 低 | 否 |
| 分析日志 | 读 | 低 | 否 |
| 修改资源申请 | 写 | 中 | 是 |
| 重新提交作业 | 写 | 中 | 是 |
| 取消作业 | 写 | 高 | 必须确认 |
| Drain 节点 | 写 | 高 | 必须确认 |

还应说明：

- 哪些结果来自系统事实查询；
- 哪些内容属于 Agent 推断；
- 哪些只是建议；
- 哪些动作已经实际执行；
- 谁拥有授权权限；
- 执行失败后如何恢复或回滚；
- 如何保留操作与审计记录。

### 4.5 设计原则与硬约束

建议写入项目级 Agent 指令：

```markdown
- Agent 结论必须附带证据和数据来源。
- 事实、推断、建议与已执行动作必须视觉区分。
- 写操作执行前必须预览影响范围。
- 长任务必须显示当前步骤、等待原因和阶段结果。
- 失败状态需要保留已经产生的部分结果。
- 无数据、无权限、未查询和查询正常必须区分。
- 告警和状态不能只靠颜色表达。
- 不伪造实时数据、工具调用和执行结果。
- Demo 中的模拟数据和模拟执行必须明确标记。
```

### 4.6 视觉系统

向 Agent 提供：

- 颜色及语义 Token；
- 排版层级；
- 间距和圆角规则；
- 表格、卡片、状态标签等组件规范；
- 图表、拓扑和状态的颜色映射；
- Agent 专属图标和状态表达；
- 深浅主题策略；
- 参考产品截图；
- 明确禁止使用的视觉模式。

---

## 5. 每一轮任务应该传递什么

长期上下文进入仓库；每一轮只向 Agent 传递当前任务的增量信息。

### 单轮任务模板

```markdown
## 本轮目标
设计“作业失败诊断”概念 Demo。

## 目标用户
应用工程师，了解基本 Linux/HPC 概念，但不熟悉调度器内部机制。

## 触发场景
用户发现作业运行 12 分钟后失败，希望判断原因并决定是否重新提交。

## 用户希望完成的决定
判断失败是否由资源申请、应用参数或节点异常引起，并决定是否修改后重提。

## 输入材料
- job.json
- stderr.log
- node-metrics.json
- 现有作业详情页截图
- 术语表和权限规则

## 必须表达
- 当前结论
- 原始证据
- 推理过程
- 置信度与未确认项
- 建议修改
- 重新提交前的差异预览和确认

## 不做
- 不连接真实调度器
- 不实际提交作业
- 本轮不探索管理员视角

## 状态覆盖
- 初始
- 分析中
- 找到原因
- 证据不足
- 工具失败
- 等待确认
- 执行完成

## 验收标准
- 用户能在 30 秒内理解主要失败原因
- 所有重要结论均可回到原始证据
- 不把推断表达为确定事实
- 未确认时不能模拟执行写操作
- 工具失败时仍保留已经获得的结果
```

---

## 6. 如何向 Agent 提供设计反馈

优先描述观察到的现象、受影响的用户任务和希望达到的结果，不要直接猜测技术原因。

### 有效反馈

- 用户看不出来这条结论是日志事实还是模型推断。
- 执行按钮出现得太早，用户还没看到影响范围。
- 当前进度只有百分比，看不出 Agent 在等待调度器还是仍在分析。
- 作业和节点信息被拆开，用户无法建立失败事件与节点状态的关联。
- 这个方案增加了信息，但没有帮助用户做出“是否重提”的决定。

### 不够有效的反馈

- 不够高级。
- 再智能一点。
- 像 AI 产品一点。
- 这里不好看。

如果是视觉问题，最好提供局部截图并说明：

1. 哪里不对；
2. 对谁造成什么问题；
3. 哪些内容必须保留；
4. 本轮只调整什么；
5. 如何判断修改已经成功。

---

## 7. 推荐的仓库结构

```text
hpc-agent-demo/
├── AGENTS.md
├── README.md
├── knowledge/
│   ├── glossary.md
│   ├── users-and-scenarios.md
│   ├── data-model.md
│   ├── domain-rules.md
│   ├── agent-capabilities.md
│   ├── permissions-and-risk.md
│   ├── visual-semantics.md
│   ├── decisions.md
│   └── open-questions.md
├── design-system/
│   ├── tokens.css
│   ├── components.css
│   └── patterns.md
├── mock-data/
├── shell/
│   └── HPC 智能体共享外壳
├── concepts/
│   └── 一个概念一个目录
├── research/
├── scripts/
│   └── 视觉、内容和结构检查
└── site/
    └── 概念启动页、目录和缩略图
```

### 重要文件的职责

- `AGENTS.md`：Agent 必须遵守的项目硬约束及工作方式。
- `knowledge/`：长期有效的 HPC 领域语境。
- `decisions.md`：记录为什么这样设计、放弃了什么、何时重新考虑。
- `open-questions.md`：尚未验证的问题，避免 Agent 把未知内容写成事实。
- `design-system/`：所有 Demo 共用的视觉语言和组件。
- `mock-data/`：可以复现不同状态的脱敏样例。
- `shell/`：稳定的共享工作台外壳。
- `concepts/`：相互独立的设计假设与实验。
- `scripts/`：把反复出现的设计问题变成自动检查。

---

## 8. 建议的协作循环

```text
1. 设计师定义目标用户、真实任务和要验证的假设
2. Agent 阅读领域材料、数据和现有界面
3. Agent 输出理解摘要、事实、推断和待确认项
4. 设计师校正领域语义与产品判断
5. 双方形成问题—目标—状态—验收的执行简报
6. Agent 先完成最小可运行版本
7. 设计师通过实际操作和截图进行评审
8. 每轮只处理一个明确问题并立即验证
9. 稳定经验进入文档、模板、Skill 或检查脚本
10. 最终生成独立 Demo、缩略图、说明和汇报材料
```

建议让 Agent 在开始实现前先输出：

- 它对任务的理解；
- 已确认事实；
- 当前假设；
- 待确认问题；
- 计划修改的文件；
- 本轮验收方法。

这样可以尽早发现语义理解偏差，避免在错误方向上完成大量页面。

---

## 9. 最需要避免的问题

### 9.1 先画聊天框，后补任务模型

后果是界面像通用 AI 助手，无法体现 HPC 对象、工具和工作流。

### 9.2 使用漂亮但不真实的 HPC 数据

错误的数据关系、数量级和状态会让概念验证失去意义。

### 9.3 把文字回答当作 Agent 的唯一产物

Agent 还应能生成结构化结论、证据链、对象联动、操作预览和审计记录。

### 9.4 每个概念重新制作一套工作台

应共享基础外壳，只改变需要验证的核心体验。

### 9.5 只设计成功状态

HPC 场景尤其需要覆盖：

- 权限不足；
- 数据缺失或过期；
- 调度器无响应；
- 日志不可用；
- 证据冲突；
- Agent 结论不足；
- 用户拒绝授权；
- 执行部分成功；
- 任务中断和恢复。

### 9.6 把高保真 Demo 当成工程可行性证明

Demo 可以验证信息表达与交互假设，但不等于底层接口、性能、安全和权限模型已经可实现。应明确标记模拟能力和工程待验证项。

---

## 10. 核心原则

在这个项目中，设计师最需要持续向 Agent 提供的不是页面长什么样，而是：

> 正确的 HPC 语境、真实的用户任务、清楚的判断标准、可信的证据要求、明确的权限边界，以及每一轮的设计取舍。

Agent 最适合负责将这些判断快速转换成可以操作、比较、验证和沟通的可运行产物。
